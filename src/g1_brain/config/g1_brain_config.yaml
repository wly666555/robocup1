g1_brain_node:
  ros__parameters:
    # 游戏相关参数
    game:
      field_size: kid
      playerStartPos: left
      location_mode: normal
    
    # 机器人相关参数
    robot:
      height: 1.3
      scale_factor: 1.4
      pitch_compensation: -45
      yaw_compensation: 0
    
    # 记忆相关参数
    memory:
      ball_memory_length: 5.0  # 球位置记忆时间（秒）
